// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    firebaseConfig: {
        apiKey: 'AIzaSyBYBfG_xju0WSUqqcSgxQVgJiopgvRtfLQ',
        authDomain: 'capstone-register-login.firebaseapp.com',
        projectId: 'capstone-register-login',
        storageBucket: 'capstone-register-login.appspot.com',
        messagingSenderId: '78158063869',
        appId: '1:78158063869:web:9e5f91f8f0fdf1fc4e7694',
    },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
